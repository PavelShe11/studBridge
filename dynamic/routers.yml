http:
  routers:
    auth-service:
      rule: "PathPrefix(`/auth`)"
      service: auth-accountGrpcService
      entryPoints:
        - web

#    user-accountGrpcService:
#      rule: "PathPrefix(`/user`)"
#      accountGrpcService: user-accountGrpcService
#      entryPoints:
#        - web
